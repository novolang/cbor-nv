# Build outputs.
_novo/

# The built package binary — a library package still produces one,
# because its manifest needs a `main`.
cbor-nv

# Generated from the sources by `novo doc`; a committed copy is a
# second original.
APIDOC.md

# Resolved deps.  There are none — the traits are the standard
# library's and the arguments are big-endian, not varints.
novo.lock
